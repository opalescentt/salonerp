ENV=local

# Points at the app role (created in the first migration), NOT the compose
# owner role — see infra/docker-compose.yml's note on RLS bypass.
DATABASE_URL=postgresql+psycopg://salonerp_app:changeme@localhost:5432/salonerp

REDIS_URL=redis://localhost:6379/0

JWT_SECRET=changeme-dev-only
JWT_ALGORITHM=HS256
